version: 2
models:
  - name: bronze_eurostat
    columns:
      - name: source
        tests: [not_null]
      - name: dataset
        tests: [not_null]
      - name: country_code
        tests: [not_null]
      - name: year
        tests: [not_null]
      - name: month
        tests: [not_null]
  - name: bronze_ecb
    columns:
      - name: source
        tests: [not_null]
      - name: series_key
        tests: [not_null]
      - name: year
        tests: [not_null]
      - name: month
        tests: [not_null]
